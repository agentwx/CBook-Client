<transition
  show="{{ show }}"
  custom-class="overlay {{customClass}}"
  custom-style="z-index: {{ zIndex }}; {{ mask ? 'background-color: rgba(0, 0, 0, .7);' : '' }}; {{ customStyle }}"
  catch:touchmove="noop"
>
</transition>
