<view
  wx:if="{{ inited }}"
  class="transition {{customClass}}"
  style="animation-name: {{ name }}-{{ type }}; animation-duration: {{ duration }}ms; animation-delay: {{ delay }}ms; {{ display ? '' : 'display: none;' }}; {{ customStyle }}"
  bindanimationend="onAnimationEnd"
>
  <slot />
</view>
