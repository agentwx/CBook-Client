<transition
  name="slide-down"
  show="{{ show }}"
  custom-class="notify"
  custom-style="background-color:{{ backgroundColor }}; color: {{ color }}"
>
  <text wx:if="{{text}}">
    {{text}}
  </text>
  <slot wx:else></slot>
</transition>
