<view class="modal {{extraClass}}" wx:if="{{isVisible}}" style="{{extraStyle}}">
  <view class="modal-dialog {{title !== '' ? 'hasTitle' : ''}}">
    <view class="modal-header" wx:if="{{title !== ''}}">
      <text>{{title}}</text>
    </view>
    <view class="modal-close" wx:if="{{isShowClose}}" bindtap="closeModal">
      <icon size="small" color="#999" name="close"></icon>
    </view>
    <view class="modal-content">
      <slot></slot>
      <view class="modal-footer">
        <slot name="footer"></slot>
      </view>
    </view>
  </view>
</view>
